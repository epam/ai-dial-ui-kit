---
analyzer:
  skip_excluded: true
excludes:
  paths:
    - pattern: 'fixtures/**'
      reason: 'TEST_OF'
      comment: >-
        Consumer fixtures verify the packed distribution and are not part of
        the published package.
