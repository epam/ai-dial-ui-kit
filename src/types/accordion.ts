/** Where an `Accordion` header draws its caret. */
export enum AccordionCaretPosition {
  /** Before the title, at the header's leading edge. */
  Start = 'start',
  /** After the title, at the header's trailing edge. */
  End = 'end',
}
